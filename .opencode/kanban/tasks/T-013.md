# bug: prevent injection on OmO injected prompts

when subagent tasks are still pending - i see stuff like this. 
this is bad no need to inject here ever.

eg:
<opencode-supernudge>
[outside-in-development] You MUST respect outside-in-development:
Outside-in development is something I prefer doing when it comes to development. It allows you to have leaner code, less wasteful code, more proper iterations, and pairs really well with test-driven development. Essentially, you start from the most outer, front user-facing or abstract class and declare the services that you need. Of course the services are not yet implemented so you'll have a build error. And so you stub the build error such that it builds and then fails your test along with TDD. So when I ask you to work outside in, that's the approach. You really go from the most abstract to the most concrete to reduce waste, waste to be more efficient and to pair really well with test-driven development.
[register-task] Before working on any user task, ensure you have registered it using the `task_create` tool.
[tdd] TDD: YOU MUST respect the RED / GREEN principle - you ALWAYS write a FAILING test case FIRST - this will ENSURE your changes actually fix the problem.
[e2e-run] Remember not only to run unit tests, but also integration/architectural/end2end tests before considering a task as complete and announcing this to the User.
[ledger-read] When you start investigating an issue, you can read the sections from the Ledger which are relevant to the task.
`{projectroot}/.opencode/ledger/{ddd-domain}.md`
Disoveries have been grouped by date (yyyy-mm-dd) under a markdown title # or ##.
This also allows you to grep sections easily using those date references and titles.
You will understand the iterative process over time
[test-pollution] Be very wary of creating too many test cases. Test the inputs and the outputs of what the user will do at the seams of the application. No more, no less. We don't want to test cases that are extreme outliers.
</opencode-supernudge>

<system-reminder>
[BACKGROUND TASK COMPLETED]
**ID:** `bg_8a707118`
**Description:** Score signoff criteria
**Duration:** 1m 20s

**1 task still in progress.** You WILL be notified when ALL complete.
Do NOT poll - continue productive work.

Use `background_output(task_id="bg_8a707118")` to retrieve this result when ready.
</system-reminder>
<!-- OMO_INTERNAL_INITIATOR -->
