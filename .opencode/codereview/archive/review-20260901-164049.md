## src/test/e2e.test.ts:500-500

````typescript
  test("AC14: given global config with NUDGE_GLOBAL and local config at ./.opencode/com.kevincojean.opencode-supernudge/supernudge-configuration.jsonc with NUDGE_LOCAL, when chat.message fires, then message contains BOTH NUDGE_GLOBAL and NUDGE_LOCAL (concatenate)", async () => {
````

what is this NUDGE_GLOBAL thing!?
and NUDGE_LOCAL thing? is it only for tests?
## src/test/supernudge.test.ts:675-675

````typescript
  "given global enabled.normalMessage=true and local enabled.normalMessage=false, when chat.message fires, then NO nudge",
````

FALSE = local nudge configuration DOES NOT override uniquely global attributes - it only affects the content of what is injected
## src/test/supernudge.test.ts:706-706

````typescript
  "given configEnabled=false and local config exists, when chat.message fires, then only global prompts",
````

false
## src/test/supernudge.test.ts:736-736

````typescript
  "given local config not found, when chat.message fires, then global config used, no toast",
````

delete test; useless
## src/test/supernudge.test.ts:812-812

````typescript
  "given local config with configFilePath or configEnabled, when merged, then those keys ignored",
````

id argue ANY key that is not explicitely listed as being locally overridable - we have to discuss this - should be ignored - if we explcicitely flag each conf variable as being locally overridable or not then that will help us long term
## src/test/supernudge.test.ts:877-877

````typescript
  "given config with per-prompt resetCounterOnCompaction=false and interval=3, when 1st message injects then compaction fires then 2nd message sent, then that prompt does NOT inject on 2nd message",
````

false
