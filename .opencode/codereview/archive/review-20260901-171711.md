## README.md:93-95

````markdown
### Project-local config

A project-local config file can overlay the global config. Two global config keys control this:
````

make this section collapsed by default and also make explicit which config keys can also be specified in the project local configs
## README.md:137-137

````markdown
### Subagent nudge paths
````

collapse by default
## README.md:153-153

````markdown
### Per-prompt override
````

collapse by default
## README.md:223-223

````markdown
## Test
````

collapse by default
## src/main/index.ts:120-122

````typescript
  // Only `prompts` is locally overridable. All other keys are global-only.
  // Per-prompt overrides (e.g. injection.interval, enabled.*) work via
  // the existing resolvePrompts spread: { ...defaults, ...obj } in each prompt entry.
````

HUH? what about the table I gave you?
## src/main/index.ts:121-122

````typescript
  // Per-prompt overrides (e.g. injection.interval, enabled.*) work via
  // the existing resolvePrompts spread: { ...defaults, ...obj } in each prompt entry.
````

this comment is very unclear
## src/test/e2e.test.ts:500-500

````typescript
  test("AC15: given global config with global prompt and local config at ./.opencode/com.kevincojean.opencode-supernudge/supernudge-configuration.jsonc with local prompt, when chat.message fires, then message contains BOTH nudges (concatenate)", async () => {
````

remove useless test.can you stop fucking over the increments in AC? the prev AC is AC13 y u keep fucking this over

## src/test/supernudge.test.ts:727-727

````typescript
  "given local config with non-prompts keys, when merged, then ALL non-prompts keys ignored",
````

in production code I DID NOT SEE ANY PLACE which specified in a HARDCODED manner which keys can be overriden and which CANNOT - I WANT THIS CENTRALIZED
